name: "Terraform Action"
description: "Run Terraform commands in a custom container action"
inputs:
  tf-command:
    description: "Terraform command to run"
    required: true
runs:
  using: "docker"
  image: "Dockerfile"
  args:
    - ${{ inputs.tf-command }}
